{
  "name": "reelrate",
  "version": "0.0.1",
  "description": "ReelRate web app",
  "license": "MIT",
  "author": "Karim Khasan",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Kkkkkk58/FrontendIntro.git"
  },
  "scripts": {
    "build": "parcel build index.html",
    "dev": "parcel index.html --open",
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.8.1"
  }
}
